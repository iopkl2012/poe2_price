# 初始创建 .gitignore 文件

## 变更时间
2026-06-19

## 变更内容
- 创建 `.gitignore` 文件
- 添加编译输出忽略规则（`build/**/bin/`、`build/**/obj/`、`build/**/publish/`）
- 添加运行时输出忽略规则（`物价补丁/output/`）
- 添加生成补丁文件忽略规则（`物价补丁/*.zip`）
- 添加临时文件忽略规则（`*.tmp`、`*.log`）
- 添加 VS Code 配置忽略规则（`.vscode/`）
- 添加下载文件忽略规则（`LibGGPK3_win-x64.zip`）

## 变更原因
避免将编译输出、运行时生成文件等不必要内容上传到 Git 仓库。